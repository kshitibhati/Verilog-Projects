module loadcounter (clk,reset,load,count,data);
input  load,clk,reset;
output reg [3:0] count;
output data;
always @ (posedge clk);
if (reset==1)
begin
count=4'b0000;
end
else if (load == 0)
begin
count=count +1;
end
else if (load == 1)
begin
count=data;
end
endmodule

