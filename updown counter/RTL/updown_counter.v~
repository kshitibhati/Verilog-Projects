module count4bitup  (clk,reset,en,count);
output reg [3:0] count;
input clk,reset,en;

always (posedge clk);

begin
if (en=1)
  (count==0,count<=4'b1111,count=count+1);
else (count==1,c>=4'b0000,count=count-1);
end

endmodule
